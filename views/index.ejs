<!DOCTYPE html>
<html lang="en">
    <%- include('./partials/head.ejs') %>
<body>
    <%- include('./partials/nav.ejs') %>
</body>

    <div class = "blogs content">
    <h2>Profiles</h2>
        <% if (dogs.length >0) { %>
        <% dogs.forEach(dog => { %>
        <h3 class = "title"><%= dog.title %></h3>
        <p class = "snippet"><%= dog.snippet %></p>

    <% }) %>
    <% } else {%>
        <p>There are no profiles to display. </p>
    <% } %>
    </div>
    <%- include('./partials/footer.ejs') %>   
</html>